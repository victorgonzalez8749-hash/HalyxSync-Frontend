package com.halyxsynck.theme

import androidx.compose.ui.graphics.Color

// Colores principales
val PrimaryBlue = Color(0xFF0D3B80)
val SecondaryCyan = Color(0xFF20D5E7)

// Fondo
val Background = Color(0xFFF4F7FC)
val Surface = Color(0xFFFFFFFF)

// Texto
val TextPrimary = Color(0xFF1E2B45)
val TextSecondary = Color(0xFF7E8798)

// Bordes
val Border = Color(0xFFD8DCE5)

// Estados
val Success = Color(0xFF2E7D32)
val Error = Color(0xFFD32F2F)

// Extras
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)

// Acentos para el dashboard del paciente (mezcla azul-morado)
val PurpleAccent = Color(0xFF6C4FD6)
val GradientStart = Color(0xFF0D3B80)
val GradientEnd = Color(0xFF6C4FD6)